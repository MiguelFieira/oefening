{% extends 'base.html.twig' %}

{% block title %}Scholen{% endblock %}

{% block body %}
    <h1>Scholen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ scholen.id }}</td>
            </tr>
            <tr>
                <th>Omschrijving</th>
                <td>{{ scholen.omschrijving }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('scholen_index') }}">back to list</a>

    <a href="{{ path('scholen_edit', {'id': scholen.id}) }}">edit</a>

    {{ include('scholen/_delete_form.html.twig') }}
{% endblock %}
